{
  "name": "@luminary/sdk-ts",
  "version": "0.1.0",
  "type": "commonjs",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --ext .ts",
    "test": "node --test",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "cross-fetch": "^3.1.5"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}